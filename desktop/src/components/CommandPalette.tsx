// Temporary stub — replaced by the real command palette in Task 21.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function CommandPalette(_: { open: boolean; onClose: () => void }) { return null; }
